{# This file is inserted as the 'footer' of each listed record. #}

    Written by <strong><a href="/author/{{record.author}}">
        {% set a = '' ~ record.author %}
        {{ attribute(rssfeeds, a).name -}}
    </a></strong>
    {% if record.source %}
    - Published on <a href="{{ record.source }}" target="_blank">
        {{ attribute(rssfeeds, a).title -}}
    </a>
    {% endif %}
    <br>
    Aggregated on {{ record.datepublish|localdate("%A %B %e, %Y")}} -
    <a href="{{ record.link }}">Permalink</a> 